doctype html
head
  meta(charset='UTF-8')
  meta(name='viewport', content='width=device-width, initial-scale=1.0')
  title Admin Page - Equipment Reservation
  style.
    body {
    font-family: 'Arial', sans-serif;
    margin: 0;
    padding: 0;
    background: #f4f4f4;
    }
    header {
    background-color: #2c3e50;
    padding: 20px;
    text-align: center;
    color: #fff;
    }
    div {
    max-width: 600px;
    margin: 20px auto;
    background: #fff;
    padding: 20px;
    border-radius: 8px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }
    h2 {
    color: #2c3e50;
    }
header
  h1 Admin Page - Equipment Reservation
div
  h2 Reserved Equipment:
  | $tool&quot;;
  | }
  | }
  | // echo &quot;&quot;;
  | // Display the reservation time
  | $reservationTime = $_POST[&quot;reservation_time&quot;];
  | echo &quot;
  p Reservation Time: $reservationTime
  | &quot;;
  |             // include(&quot;login.php&quot;);
  |         } else {
  |             echo &quot;
  p No reservation data received.
  | &quot;;
  |         }
  |         ?&gt;
